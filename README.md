# Escape Maze — Arrow Puzzle (React)

Polished React implementation of an Arrow Maze puzzle game (ditto style). Includes Vite + Tailwind + Framer Motion and GitHub Actions workflow for deployment.

## Quick start

```bash
# install
npm ci
# dev
npm run dev
# build
npm run build
```

## Android (Capacitor)
See `build_apk.sh` for steps to generate an Android APK from the web build.
