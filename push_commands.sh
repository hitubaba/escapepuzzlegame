#!/bin/bash
# Replace <your-username> below before running
git init
git branch -M main
git add .
git commit -m "Initial commit — Escape Maze"
git remote add origin https://github.com/<your-username>/escapepuzzlegame.git
git push -u origin main
